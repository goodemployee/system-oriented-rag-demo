Application 路徑介紹

它是：

    系統的流程協調層（Use Cases / Application Services）

    將 core 與 capabilities 組合成可執行流程

    API 與核心邏輯之間的邊界


它不是：

    不是 core/domain（不定義核心規則）

    不是 infrastructure（不直接接觸外部系統）

    不是 capability（不提供可重用能力）